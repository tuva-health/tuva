version: 2

models:
  - name: eligibility
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [claims, input_layer]
    columns:
      - name: x_first_name
        description: Extension fixture declared like a connector-owned column contract.

  - name: medical_claim
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [claims, input_layer]

  - name: pharmacy_claim
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [claims, input_layer]

  - name: provider_attribution
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [attribution, input_layer]

  - name: appointment
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: condition
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: encounter
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: immunization
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: lab_result
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: location
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: medication
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: observation
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: patient
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: practitioner
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]

  - name: procedure
    description: Created from the Tuva synthetic data seed table.
    config:
      tags: [clinical, input_layer]
