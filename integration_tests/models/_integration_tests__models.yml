version: 2

models:
# claim input models
  - name: eligibility
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - claims
        - input_layer
      materialized: table

  - name: medical_claim
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - claims
        - input_layer
      materialized: table

  - name: pharmacy_claim
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - claims
        - input_layer
      materialized: table

  - name: provider_attribution
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - attribution
        - input_layer
      materialized: table


# clinical input models
  - name: appointment
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: condition
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: encounter
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: immunization
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: lab_result
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: location
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: medication
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: observation
    description: Created from synthetic seed data.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: patient
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: practitioner
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table

  - name: procedure
    description: Created as an empty input-layer table for synthetic integration tests.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}_{{var('tuva_schema_prefix')}}input_layer
        {% else %}input_layer{%- endif -%}
      tags:
        - clinical
        - input_layer
      materialized: table
