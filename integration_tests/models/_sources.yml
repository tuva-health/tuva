version: 2

sources:
  - name: claims_input
#    database: "{{ var('claims_preprocessing_input_database','tuva') }}"
    schema:  "{{ var('claims_preprocessing_input_schema','input_layer') }}"
    tables:
      - name: medical_claim
      - name: eligibility
      - name: pharmacy_claim

