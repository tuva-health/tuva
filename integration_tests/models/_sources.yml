version: 2

sources:
  - name: claims_input
    database: "{{ var('input_database') }}"
    schema:  "{{ var('input_schema') }}"
    tables:
      - name: medical_claim
      - name: eligibility
      - name: pharmacy_claim
