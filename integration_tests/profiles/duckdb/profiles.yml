default:
  target: ci
  outputs:
    dev:
      type: duckdb
      path: dev.duckdb
      threads: 4
    ci:
      type: duckdb
      path: "{{ env_var('DBT_MOTHERDUCK_CI_PATH') }}"
      threads: 4

