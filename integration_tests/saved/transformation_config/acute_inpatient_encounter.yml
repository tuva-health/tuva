version: 2

models:
    - name: acute_inpatient_encounter
      description: Mapping of acute inpatient encounter
      config:
        database: "{{ var('output_database')}}"
        schema: "{{ var('output_schema')}}"
