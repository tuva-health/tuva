version: 2

models:
    - name: emergency_department_encounter
      description: Mapping of ED encounters
      config:
        database: "{{ var('output_database')}}"
        schema: "{{ var('output_schema')}}"
