name: 'the_tuva_project'
version: '0.1.10'
config-version: 2

profile: default        # configuration required (your profile to connect dbt to your data warehouse)


model-paths: ["models"]
analysis-paths: ["analysis"]
test-paths: ["tests"]
seed-paths: ["data"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]
target-path: "target"  
clean-targets:         
    - "target"
    - "dbt_modules"

