version: 2

models:
  - name: epic__attribution
    description: "Attribution table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__claim_header
    description: "Claim header table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__demographics
    description: "Demographics table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__diagnoses
    description: "Diagnoses table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__eligibility
    description: "Eligibility table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__medications
    description: "Medications table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__procedures
    description: "Procedures table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__service_line
    description: "Service line table for the EPIC extract"
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

  - name: epic__contract_id
    description: generate contract id per spec
    config:
      materialized: table
      schema: epic_extract
      tags: epic_extract

