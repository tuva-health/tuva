version: 2

models:
    - name: chronic_conditions_wide
      description: >
        A patient-level table (i.e. one record per patient) with flags for each chronic condition.

      columns:
          - name: patient_id
            description: >
              Unique ID for the patient.
                

