version: 2
models:
- name: ccsr__stg_core__condition
  config:
    tags:
    - ccsr
    materialized: ephemeral
  columns:
  - name: encounter_id
  - name: claim_id
  - name: patient_id
  - name: normalized_code
  - name: condition_rank

