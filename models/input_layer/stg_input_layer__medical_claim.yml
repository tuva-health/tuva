version: 2

# Schema vocabulary for this file. Every model config points at one of
# these, so a schema is defined once per file rather than once per model.
anchors:
  - &schema_staging |
    {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_staging{%- else -%}staging{%- endif -%}

models:
  - name: stg_input_layer__medical_claim
    description: >-
      The medical_claim table is intended to have one record per medical claim line per data source. A medical claim represents healthcare services or supplies billed by a provider and adjudicated by a payer. A single claim may have multiple claim lines, so claim_id identifies the claim and claim_line_number identifies the specific line within that claim.


      This table should contain final adjudicated claim lines after the source data has been filtered or transformed to account for adjustments, denials, reversals, voids, and replacement claims. If non-final claim activity is included, downstream cost, utilization, quality, and risk adjustment outputs may be incorrect. Header-level fields, such as claim_type, person_id, payer, plan, claim_start_date, claim_end_date, admission_date, and discharge_date, should be consistent across all lines for the same claim_id. Line-level fields, such as claim_line_start_date, claim_line_end_date, revenue_center_code, hcpcs_code, service_unit_quantity, and payment amounts, may vary across claim lines.


      Primary key: claim_id, claim_line_number, data_source.
    config:
      schema: *schema_staging
      tags: input_layer
      materialized: view
    columns:
      - name: claim_id
        description: >-
          Identifier for a medical claim. A claim may contain multiple claim lines, so the same claim_id may appear on multiple rows. This value should be populated on every row and should remain consistent across all lines for the same claim.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: claim_line_number
        description: >-
          Line number for the claim line represented by the row. Values should be positive integers and should uniquely identify each line within a claim_id. If the source does not provide reliable line numbers, create them during mapping.
        config:
          meta:
            data_type: integer
            is_primary_key: true
      - name: claim_type
        description: >-
          Header-level classification of the medical claim as professional, institutional, or undetermined. This value should be consistent across all lines for the same claim_id. If claim_type is not provided by the source, derive it from claim content: professional claims should have a place_of_service_code and should not have institutional-only fields such as bill_type_code or revenue_center_code; institutional claims should have bill_type_code and revenue_center_code and should not have place_of_service_code. If the source does not clearly support either pattern, use undetermined until the classification can be resolved. Some payers populate institutional claims with place_of_service_code values or create nonstandard place of service codes, so review source-specific behavior carefully before deriving claim_type.
        config:
          meta:
            data_type: varchar
      - name: person_id
        description: >-
          Globally unique identifier for the person who received care. A person may have multiple member IDs over time or across plans, but person_id should remain stable for the same person.
        config:
          meta:
            data_type: varchar
      - name: member_id
        description: >-
          Identifier assigned by the payer or health plan for the person's coverage. This should identify the member associated with the claim and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: payer
        description: >-
          Name of the payer responsible for the claim, for example Aetna, Blue Cross Blue Shield, or Medicare FFS. If the source data is scoped to one payer, this value may be hard-coded.
        config:
          meta:
            data_type: varchar
      - name: plan
        description: >-
          Name of the health insurance plan or product associated with the claim, for example Aetna PPO or Humana HMO. If unavailable in the source, this value may be hard-coded to a stable plan value for the data source.
        config:
          meta:
            data_type: varchar
      - name: claim_start_date
        description: >-
          Header-level start date for the claim, formatted as YYYY-MM-DD. This value should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: date
      - name: claim_end_date
        description: >-
          Header-level end date for the claim, formatted as YYYY-MM-DD. This value should be consistent across all lines for the same claim_id and should be greater than or equal to claim_start_date.
        config:
          meta:
            data_type: date
      - name: claim_line_start_date
        description: >-
          Start date for the service or item represented by the claim line, formatted as YYYY-MM-DD. This may differ across lines within the same claim.
        config:
          meta:
            data_type: date
      - name: claim_line_end_date
        description: >-
          End date for the service or item represented by the claim line, formatted as YYYY-MM-DD. This may differ across lines within the same claim and should be greater than or equal to claim_line_start_date.
        config:
          meta:
            data_type: date
      - name: admission_date
        description: >-
          Header-level admission date for an inpatient institutional claim, formatted as YYYY-MM-DD. Populate when the claim represents an inpatient facility stay. This value should be consistent across all lines for the same inpatient institutional claim.
        config:
          meta:
            data_type: date
      - name: discharge_date
        description: >-
          Header-level discharge date for an inpatient institutional claim, formatted as YYYY-MM-DD. Populate when the claim represents an inpatient facility stay. This value should be consistent across all lines for the same inpatient institutional claim and should be greater than or equal to admission_date.
        config:
          meta:
            data_type: date
      - name: admit_source_code
        description: >-
          Header-level code indicating the source of admission for an inpatient institutional claim. This value should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: admit_type_code
        description: >-
          Header-level code indicating the priority or type of admission for an inpatient institutional claim, such as emergency, urgent, or elective. This value should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: discharge_disposition_code
        description: >-
          Header-level code indicating where the person was discharged after an inpatient institutional stay. This value should be consistent across all lines for the same claim_id. Preserve leading zeroes when present in the standard code set.
        config:
          meta:
            data_type: varchar
      - name: place_of_service_code
        description: >-
          Line-level code indicating where a professional service was provided. Professional claim lines should generally have a place_of_service_code; institutional claims should never have a place_of_service_code. Some payers create nonstandard place of service codes or populate institutional claims with place_of_service_code values even though this is not technically allowed, so review source-specific behavior carefully during mapping.
        config:
          meta:
            data_type: varchar
      - name: bill_type_code
        description: >-
          Header-level bill type code for institutional claims. This identifies the type of facility claim and should be consistent across all lines for the same claim_id. Preserve leading zeroes when present in the standard code set.
        config:
          meta:
            data_type: varchar
      - name: drg_code_type
        description: >-
          DRG coding system for drg_code. Accepted values are ms-drg and apr-drg. Populate when drg_code is populated.
        config:
          meta:
            data_type: varchar
      - name: drg_code
        description: >-
          Diagnosis Related Group code for an inpatient claim. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: revenue_center_code
        description: >-
          Line-level revenue center code for institutional claims. This identifies the department or service area associated with the claim line and should preserve leading zeroes when present in the standard code set.
        config:
          meta:
            data_type: varchar
      - name: service_unit_quantity
        description: >-
          Number of service units for the claim line. This may represent units associated with a revenue center, procedure, service, or supply.
        config:
          meta:
            data_type: number
      - name: hcpcs_code
        description: >-
          CPT or HCPCS code for the procedure, service, or supply on the claim line. This is line-level information and may vary across lines within the same claim.
        config:
          meta:
            data_type: varchar
      - name: hcpcs_modifier_1
        description: >-
          Modifier 1 applied to the claim line hcpcs_code. Modifiers provide additional context about how or where the service was performed. Leave null when not present.
        config:
          meta:
            data_type: varchar
      - name: hcpcs_modifier_2
        description: >-
          Modifier 2 applied to the claim line hcpcs_code. Modifiers provide additional context about how or where the service was performed. Leave null when not present.
        config:
          meta:
            data_type: varchar
      - name: hcpcs_modifier_3
        description: >-
          Modifier 3 applied to the claim line hcpcs_code. Modifiers provide additional context about how or where the service was performed. Leave null when not present.
        config:
          meta:
            data_type: varchar
      - name: hcpcs_modifier_4
        description: >-
          Modifier 4 applied to the claim line hcpcs_code. Modifiers provide additional context about how or where the service was performed. Leave null when not present.
        config:
          meta:
            data_type: varchar
      - name: hcpcs_modifier_5
        description: >-
          Modifier 5 applied to the claim line hcpcs_code. Modifiers provide additional context about how or where the service was performed. Leave null when not present.
        config:
          meta:
            data_type: varchar
      - name: rendering_npi
        description: >-
          NPI for the practitioner or provider who rendered the service on the claim line. This is generally line-level and may vary across lines. NPIs should be 10-digit identifiers.
        config:
          meta:
            data_type: varchar
      - name: rendering_tin
        description: >-
          Tax Identification Number for the rendering provider associated with the claim line.
        config:
          meta:
            data_type: varchar
      - name: billing_npi
        description: >-
          NPI for the billing provider or organization responsible for submitting the claim. This is generally header-level and should be consistent across lines for the same claim_id. NPIs should be 10-digit identifiers.
        config:
          meta:
            data_type: varchar
      - name: billing_tin
        description: >-
          Tax Identification Number for the billing provider or organization.
        config:
          meta:
            data_type: varchar
      - name: facility_npi
        description: >-
          NPI for the facility where services were delivered. This is especially important for institutional claims and encounter grouping. NPIs should be 10-digit identifiers.
        config:
          meta:
            data_type: varchar
      - name: paid_date
        description: >-
          Date the claim was paid by the payer, formatted as YYYY-MM-DD. This often occurs after claim_end_date.
        config:
          meta:
            data_type: date
      - name: paid_amount
        description: >-
          Amount paid by the payer for the claim line. This should reflect final adjudicated payment after adjustment, denial, reversal, and replacement logic has been applied.
        config:
          meta:
            data_type: float
      - name: allowed_amount
        description: >-
          Total allowed amount for the claim line, including payer-paid and patient-responsibility amounts. This is generally expected to equal paid_amount plus coinsurance_amount plus copayment_amount plus deductible_amount when those fields are available.
        config:
          meta:
            data_type: float
      - name: charge_amount
        description: >-
          Amount charged or billed by the provider for the claim line before payer adjudication.
        config:
          meta:
            data_type: float
      - name: coinsurance_amount
        description: >-
          Coinsurance amount owed by the member for the claim line. This represents percentage-based member cost sharing.
        config:
          meta:
            data_type: float
      - name: copayment_amount
        description: >-
          Copayment amount owed by the member for the claim line. This represents fixed-dollar member cost sharing.
        config:
          meta:
            data_type: float
      - name: deductible_amount
        description: >-
          Deductible amount owed by the member for the claim line before payer coverage applies.
        config:
          meta:
            data_type: float
      - name: total_cost_amount
        description: >-
          Total cost amount for the claim line. This may equal allowed_amount, but can differ when the source includes additional cost components not captured by the standard payer-paid and patient-responsibility fields.
        config:
          meta:
            data_type: float
      - name: diagnosis_code_type
        description: >-
          Coding system used for diagnosis codes on the claim. Accepted values are icd-9-cm and icd-10-cm. This is header-level and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_1
        description: >-
          Primary or first-listed ICD diagnosis code on the claim. For institutional claims, this should generally be the principal diagnosis. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_2
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 2. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_3
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 3. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_4
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 4. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_5
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 5. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_6
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 6. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_7
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 7. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_8
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 8. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_9
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 9. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_10
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 10. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_11
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 11. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_12
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 12. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_13
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 13. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_14
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 14. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_15
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 15. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_16
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 16. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_17
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 17. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_18
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 18. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_19
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 19. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_20
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 20. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_21
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 21. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_22
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 22. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_23
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 23. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_24
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 24. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_code_25
        description: >-
          Additional ICD diagnosis code on the claim in diagnosis position 25. Leave null when that diagnosis position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_1
        description: >-
          Present-on-admission indicator for diagnosis_code_1. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_2
        description: >-
          Present-on-admission indicator for diagnosis_code_2. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_3
        description: >-
          Present-on-admission indicator for diagnosis_code_3. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_4
        description: >-
          Present-on-admission indicator for diagnosis_code_4. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_5
        description: >-
          Present-on-admission indicator for diagnosis_code_5. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_6
        description: >-
          Present-on-admission indicator for diagnosis_code_6. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_7
        description: >-
          Present-on-admission indicator for diagnosis_code_7. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_8
        description: >-
          Present-on-admission indicator for diagnosis_code_8. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_9
        description: >-
          Present-on-admission indicator for diagnosis_code_9. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_10
        description: >-
          Present-on-admission indicator for diagnosis_code_10. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_11
        description: >-
          Present-on-admission indicator for diagnosis_code_11. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_12
        description: >-
          Present-on-admission indicator for diagnosis_code_12. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_13
        description: >-
          Present-on-admission indicator for diagnosis_code_13. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_14
        description: >-
          Present-on-admission indicator for diagnosis_code_14. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_15
        description: >-
          Present-on-admission indicator for diagnosis_code_15. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_16
        description: >-
          Present-on-admission indicator for diagnosis_code_16. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_17
        description: >-
          Present-on-admission indicator for diagnosis_code_17. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_18
        description: >-
          Present-on-admission indicator for diagnosis_code_18. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_19
        description: >-
          Present-on-admission indicator for diagnosis_code_19. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_20
        description: >-
          Present-on-admission indicator for diagnosis_code_20. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_21
        description: >-
          Present-on-admission indicator for diagnosis_code_21. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_22
        description: >-
          Present-on-admission indicator for diagnosis_code_22. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_23
        description: >-
          Present-on-admission indicator for diagnosis_code_23. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_24
        description: >-
          Present-on-admission indicator for diagnosis_code_24. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: diagnosis_poa_25
        description: >-
          Present-on-admission indicator for diagnosis_code_25. Populate for inpatient institutional claims when available. Leave null when unavailable or not applicable.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_type
        description: >-
          Coding system used for ICD procedure codes on the claim. Accepted values are icd-9-pcs and icd-10-pcs. Populate when ICD procedure codes are present.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_1
        description: >-
          Primary or first-listed ICD procedure code on the claim. This is generally used for inpatient institutional claims and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_2
        description: >-
          Additional ICD procedure code on the claim in procedure position 2. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_3
        description: >-
          Additional ICD procedure code on the claim in procedure position 3. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_4
        description: >-
          Additional ICD procedure code on the claim in procedure position 4. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_5
        description: >-
          Additional ICD procedure code on the claim in procedure position 5. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_6
        description: >-
          Additional ICD procedure code on the claim in procedure position 6. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_7
        description: >-
          Additional ICD procedure code on the claim in procedure position 7. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_8
        description: >-
          Additional ICD procedure code on the claim in procedure position 8. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_9
        description: >-
          Additional ICD procedure code on the claim in procedure position 9. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_10
        description: >-
          Additional ICD procedure code on the claim in procedure position 10. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_11
        description: >-
          Additional ICD procedure code on the claim in procedure position 11. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_12
        description: >-
          Additional ICD procedure code on the claim in procedure position 12. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_13
        description: >-
          Additional ICD procedure code on the claim in procedure position 13. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_14
        description: >-
          Additional ICD procedure code on the claim in procedure position 14. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_15
        description: >-
          Additional ICD procedure code on the claim in procedure position 15. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_16
        description: >-
          Additional ICD procedure code on the claim in procedure position 16. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_17
        description: >-
          Additional ICD procedure code on the claim in procedure position 17. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_18
        description: >-
          Additional ICD procedure code on the claim in procedure position 18. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_19
        description: >-
          Additional ICD procedure code on the claim in procedure position 19. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_20
        description: >-
          Additional ICD procedure code on the claim in procedure position 20. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_21
        description: >-
          Additional ICD procedure code on the claim in procedure position 21. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_22
        description: >-
          Additional ICD procedure code on the claim in procedure position 22. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_23
        description: >-
          Additional ICD procedure code on the claim in procedure position 23. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_24
        description: >-
          Additional ICD procedure code on the claim in procedure position 24. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_code_25
        description: >-
          Additional ICD procedure code on the claim in procedure position 25. Leave null when that procedure position is not present. This is header-level information and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
      - name: procedure_date_1
        description: >-
          Date associated with procedure_code_1, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_2
        description: >-
          Date associated with procedure_code_2, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_3
        description: >-
          Date associated with procedure_code_3, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_4
        description: >-
          Date associated with procedure_code_4, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_5
        description: >-
          Date associated with procedure_code_5, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_6
        description: >-
          Date associated with procedure_code_6, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_7
        description: >-
          Date associated with procedure_code_7, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_8
        description: >-
          Date associated with procedure_code_8, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_9
        description: >-
          Date associated with procedure_code_9, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_10
        description: >-
          Date associated with procedure_code_10, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_11
        description: >-
          Date associated with procedure_code_11, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_12
        description: >-
          Date associated with procedure_code_12, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_13
        description: >-
          Date associated with procedure_code_13, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_14
        description: >-
          Date associated with procedure_code_14, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_15
        description: >-
          Date associated with procedure_code_15, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_16
        description: >-
          Date associated with procedure_code_16, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_17
        description: >-
          Date associated with procedure_code_17, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_18
        description: >-
          Date associated with procedure_code_18, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_19
        description: >-
          Date associated with procedure_code_19, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_20
        description: >-
          Date associated with procedure_code_20, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_21
        description: >-
          Date associated with procedure_code_21, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_22
        description: >-
          Date associated with procedure_code_22, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_23
        description: >-
          Date associated with procedure_code_23, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_24
        description: >-
          Date associated with procedure_code_24, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: procedure_date_25
        description: >-
          Date associated with procedure_code_25, formatted as YYYY-MM-DD. Leave null when the corresponding procedure code or date is unavailable.
        config:
          meta:
            data_type: date
      - name: in_network_flag
        description: >-
          Indicator for whether the claim was in network. Accepted values are 1 for in network, 0 for out of network, and null when unknown.
        config:
          meta:
            data_type: boolean

      - name: file_name
        description: >-
          Name of the source file from which the claim line originated. This field is useful for tracing source records across file-based claims extracts.
        config:
          meta:
            data_type: varchar
      - name: file_date
        description: >-
          Date associated with the source file, formatted as YYYY-MM-DD. This may represent the delivery date, reporting period, or date embedded in the file name.
        config:
          meta:
            data_type: date
      - name: ingest_datetime
        description: >-
          Date and time the source claim line or file was loaded into the data warehouse. This is useful when multiple versions of the same claim line are received over time.
        config:
          meta:
            data_type: timestamp

      - name: data_source
        description: >-
          User-defined name that identifies the source of the medical claim data, for example BCBS Tennessee, Medicare FFS, or Medicaid Ohio. This value should be populated on every record and should be consistent across all lines for the same claim_id.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
