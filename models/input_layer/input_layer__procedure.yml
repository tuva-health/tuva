version: 2
models:
  - name: input_layer__procedure
    description: |-
      The procedure table contains one record per procedure per data source. It stores clinical procedures performed for a patient, including the person, patient, encounter, procedure date, source procedure code, optional modifiers, and practitioner who performed or documented the procedure.

      The source_procedure_id should identify a unique procedure record within a source system. Procedure codes should be mapped to code_system and source_code; Tuva derives normalized procedure fields downstream in core.procedure when the source code can be matched to supported terminology.

      Primary key: source_procedure_id, data_source.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_input_layer{% else %}input_layer{%- endif -%}
      tags: input_layer
      materialized: view
    columns:
      - name: source_procedure_id
        description: Source-system identifier for the procedure record. This value should be unique within a data source. Tuva uses this field to create the synthetic procedure_id in core.procedure.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: person_id
        description: Globally unique identifier for the person across the dataset.
        config:
          meta:
            data_type: varchar
      - name: patient_id
        description: Clinical source-system patient identifier, such as medical record number or MRN.
        config:
          meta:
            data_type: varchar
      - name: encounter_id
        description: Source-system identifier for the clinical encounter associated with the procedure, when available.
        config:
          meta:
            data_type: varchar
      - name: procedure_date
        description: Date the procedure was performed or documented.
        config:
          meta:
            data_type: date
      - name: code_system
        description: Source procedure code system. Expected values are snomed-ct, hcpcs, icd-10-pcs, or icd-9-pcs.
        config:
          meta:
            data_type: varchar
      - name: source_code
        description: Procedure code from the source system.
        config:
          meta:
            data_type: varchar
      - name: source_description
        description: Source-system description of the procedure code.
        config:
          meta:
            data_type: varchar
      - name: modifier_1
        description: First procedure code modifier, when applicable.
        config:
          meta:
            data_type: varchar
      - name: modifier_2
        description: Second procedure code modifier, when applicable.
        config:
          meta:
            data_type: varchar
      - name: modifier_3
        description: Third procedure code modifier, when applicable.
        config:
          meta:
            data_type: varchar
      - name: modifier_4
        description: Fourth procedure code modifier, when applicable.
        config:
          meta:
            data_type: varchar
      - name: modifier_5
        description: Fifth procedure code modifier, when applicable.
        config:
          meta:
            data_type: varchar
      - name: practitioner_id
        description: Source-system practitioner identifier for the clinician associated with the procedure.
        config:
          meta:
            data_type: varchar
      - name: ingest_datetime
        description: Date and time the record was ingested or refreshed.
        config:
          meta:
            data_type: timestamp
      - name: data_source
        description: User-configured source system identifier, such as Epic, Athena Health, or Cerner. This field is part of the primary key with source_procedure_id.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
