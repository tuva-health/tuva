version: 2
models:
- name: ahrq_measures__int_pqi_shared_exclusion_union
  config:
    schema: |
      {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_ahrq_measures{% else %}ahrq_measures{%- endif -%}
    alias: _int_pqi_shared_exclusion_union
    tags:
    - ahrq_measures
    - pqi
    materialized: view
  description: >
    Union of all exclusion criteria used across PQI measures for consistent exclusion
    logic.
  columns:
  - name: test
    description: test

## Staging

