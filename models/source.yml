version: 2

sources:
  - name: "{{ var('source_name') }}"
    database: "{{ var('source_database') }}"
    schema: "{{ var('source_schema') }}"
    tables:
      - name: patient
      - name: coverage
      - name: encounter              
      - name: practitioner
      - name: location
      - name: condition
      - name: procedure
      - name: lab              
      - name: medication
      - name: allergy
      - name: vital_sign              
      - name: observation