version: 2

sources:
  - name: tuva_core_staging
    database: "{{ var('source_database') }}"
    schema: "{{ var('source_schema') }}"
    tables:
      - name: "{{ var('src_patient_table_name') }}"
      - name: coverage
      - name: encounter              
      - name: practitioner
      - name: location
      - name: condition
      - name: procedure
      - name: lab              
      - name: medication
      - name: allergy
      - name: vital_sign              
      - name: observation