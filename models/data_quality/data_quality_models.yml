version: 2

models:
  - name: data_quality__structural
    description: One row per data source and enabled Input Layer Model with four structural readiness results for required columns, data types, population, and primary keys.
    config:
      access: public
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - data_source
              - input_table_name
    columns:
      - name: data_source
        description: Source identifier evaluated independently within its Input Layer domain. Null identifies records whose data_source is null, or the fallback used when an enabled domain has no non-null source values.
      - name: input_table_name
        description: Connector-owned Input Layer Model evaluated by this row.
        tests:
          - not_null
      - name: columns_exist
        description: Pass when every column required by the Input Layer contract exists, fail when at least one is missing, or not evaluated when the check cannot run.
        tests: &structural_result_tests
          - not_null
          - accepted_values:
              arguments:
                values:
                  - pass
                  - fail
                  - not evaluated
      - name: data_types_correct
        description: Pass when every required column uses a compatible portable data type, fail when at least one present column is incompatible, or not evaluated when a required column is missing.
        tests: *structural_result_tests
      - name: table_populated
        description: Pass when at least one record exists for this Input Layer Model and data source, fail when the count is zero, or not evaluated when data_source cannot be evaluated.
        tests: *structural_result_tests
      - name: primary_key_correct
        description: Pass when the source-scoped composite key is non-null and unique, fail when null or duplicate key records exist, or not evaluated when a prerequisite is missing or the population is empty.
        tests: *structural_result_tests
      - name: row_count
        description: Number of records in this Input Layer Model for this data source. Zero is retained for domain-roster sources absent from the Model.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0

  - name: data_quality__structural_expected_columns
    description: Internal implementation relation with one row per enabled Input Layer column expected by the Tuva contract, generated from Input Layer YAML metadata. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_expected_columns
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - model_name
              - column_name
    columns:
      - name: input_layer_domain
        tests:
          - not_null
      - name: table_name
        tests:
          - not_null
      - name: model_name
        tests:
          - not_null
      - name: column_name
        tests:
          - not_null
      - name: expected_data_type
        tests:
          - not_null
      - name: expected_type_family
        tests:
          - not_null
      - name: is_primary_key
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - "yes"
                  - "no"
      - name: column_order
        tests:
          - not_null

  - name: data_quality__structural_actual_columns
    description: Internal implementation relation with one row per actual warehouse column for each enabled Input Layer Model. It preserves the adapter-reported physical identifier and normalized type metadata, and is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_actual_columns
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - model_name
              - column_name
    columns:
      - name: input_layer_domain
        tests:
          - not_null
      - name: table_name
        tests:
          - not_null
      - name: model_name
        tests:
          - not_null
      - name: column_name
        tests:
          - not_null
      - name: actual_column_name
        tests:
          - not_null
      - name: actual_data_type
        tests:
          - not_null
      - name: actual_type_family
        tests:
          - not_null

  - name: data_quality__structural_source_populations
    description: Internal implementation relation with one row per enabled Input Layer Model and actual or fallback data source, including its record count. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_source_populations
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - model_name
              - data_source_key
    columns:
      - name: input_layer_domain
        tests:
          - not_null
      - name: data_source
      - name: data_source_key
        tests:
          - not_null
      - name: table_name
        tests:
          - not_null
      - name: model_name
        tests:
          - not_null
      - name: row_count
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0

  - name: data_quality__structural_data_sources
    description: Internal implementation relation with the shared non-null data-source roster for each enabled Input Layer domain, or one null fallback when the domain has no non-null source. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_data_sources
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - input_layer_domain
              - data_source_key
    columns:
      - name: input_layer_domain
        tests:
          - not_null
      - name: data_source
      - name: data_source_key
        tests:
          - not_null

  - name: data_quality__structural_evaluation_scope
    description: Internal implementation relation containing the complete domain-scoped set of Input Layer Model and data-source combinations Structural Data Quality evaluates, including zero-count combinations and actual null-source populations. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_evaluation_scope
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - input_table_name
              - data_source_key
    columns:
      - name: input_layer_domain
        tests:
          - not_null
      - name: input_table_name
        tests:
          - not_null
      - name: model_name
        tests:
          - not_null
      - name: data_source
      - name: data_source_key
        tests:
          - not_null
      - name: row_count
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0

  - name: data_quality__structural_column_details
    description: Internal implementation relation with one source-neutral row per enabled Input Layer Model and required column, containing expected-versus-actual metadata used to calculate column-presence and data-type results. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_column_details
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - input_table_name
              - column_name
    columns:
      - name: input_table_name
        tests:
          - not_null
      - name: column_name
        tests:
          - not_null
      - name: expected_data_type
        tests:
          - not_null
      - name: actual_column_name
      - name: actual_data_type
      - name: is_primary_key
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - "yes"
                  - "no"
      - name: column_exists
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - "yes"
                  - "no"
      - name: data_type_correct
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - "yes"
                  - "no"
      - name: column_order
        tests:
          - not_null

  - name: data_quality__structural_primary_key_tests
    description: Internal implementation relation containing primary-key subcheck counts for each data source and enabled Input Layer Model. Null test_result values identify blocked subchecks; duplicate counts include every record in a non-unique key group rather than only records beyond the first. This is not a stable public consumer contract.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_primary_key_tests
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - data_source
              - input_table_name
              - column_name
              - test
    columns:
      - name: data_source
      - name: input_table_name
        tests:
          - not_null
      - name: column_name
        tests:
          - not_null
      - name: test
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - not null
                  - duplicate value
      - name: test_result
        tests:
          - dbt_utils.accepted_range:
              arguments:
                min_value: 0

  - name: data_quality__structural_missing_columns
    description: Failure-only detail with one row for each required column missing from an enabled Input Layer Model's Warehouse Table or View. Schema evidence is source-neutral because every data source in a Model shares the same columns.
    config:
      access: public
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_missing_columns
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - input_table_name
              - column_name
    columns:
      - name: input_table_name
        description: Input Layer Model with the missing required column.
        tests:
          - not_null
      - name: column_name
        description: Required column that is absent from the Warehouse Table or View.
        tests:
          - not_null
      - name: expected_data_type
        description: Portable data type declared for the missing column in Tuva's Input Layer contract.
        tests:
          - not_null

  - name: data_quality__structural_data_type_mismatches
    description: Failure-only detail with one row for each present required column whose actual warehouse data type does not satisfy Tuva's expected type. Known mismatches remain visible when another required column is missing; missing columns themselves are reported by structural_missing_columns.
    config:
      access: public
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_data_type_mismatches
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - input_table_name
              - column_name
    columns:
      - name: input_table_name
        description: Input Layer Model with the incompatible column type.
        tests:
          - not_null
      - name: column_name
        description: Required column whose actual warehouse type is incompatible.
        tests:
          - not_null
      - name: expected_data_type
        description: Portable data type required by Tuva's Input Layer contract.
        tests:
          - not_null
      - name: actual_data_type
        description: Warehouse-specific data type reported for the physical column.
        tests:
          - not_null

  - name: data_quality__structural_primary_key_failure_counts
    description: Failure-only primary-key detail with null-value counts by key column and duplicate-record counts for the complete source-scoped composite key.
    config:
      access: public
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_primary_key_failure_counts
      tags:
        - data_quality
      materialized: table
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - data_source
              - input_table_name
              - failure_type
              - primary_key_columns
    columns:
      - name: data_source
        description: Source identifier for the failed source-scoped primary-key check. Null represents records whose data_source value is null.
      - name: input_table_name
        description: Input Layer Model containing the primary-key failure.
        tests:
          - not_null
      - name: failure_type
        description: null_value for a single primary-key column or duplicate_value for the complete source-scoped composite key.
        tests:
          - not_null
          - accepted_values:
              arguments:
                values:
                  - null_value
                  - duplicate_value
      - name: primary_key_columns
        description: One key column for a null_value failure, or the full comma-separated composite key in contract order for duplicate_value.
        tests:
          - not_null
      - name: failed_record_count
        description: Number of records with a null value in the named key column, or number of records belonging to duplicate key groups. Null and duplicate counts can overlap and must not be added together.
        tests:
          - not_null
          - dbt_utils.accepted_range:
              arguments:
                min_value: 1

  - name: data_quality__medical_claim_line_flags
    description: One row per medical claim line per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: medical_claim_line_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: claim_line_number
      - name: data_source

  - name: data_quality__medical_claim_claim_flags
    description: One row per medical claim per data source with claim-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: medical_claim_claim_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: data_source

  - name: data_quality__pharmacy_claim_line_flags
    description: One row per pharmacy claim line per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: pharmacy_claim_line_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: claim_line_number
      - name: data_source

  - name: data_quality__pharmacy_claim_claim_flags
    description: One row per pharmacy claim per data source with claim-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: pharmacy_claim_claim_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: data_source

  - name: data_quality__eligibility_span_flags
    description: One row per stable eligibility span identity, keyed by person, member, enrollment start date, payer, plan, and data source, with tri-state logical flags where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: eligibility_span_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: member_id
      - name: enrollment_start_date
      - name: payer
      - name: plan
      - name: data_source

  - name: data_quality__eligibility_person_flags
    description: One row per person per data source with tri-state logical flags evaluated across eligibility spans, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: eligibility_person_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: data_source

  - name: data_quality__appointment_flags
    description: One row per clinical appointment input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: appointment_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: appointment_id
      - name: data_source

  - name: data_quality__condition_flags
    description: One row per clinical condition input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: condition_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: source_condition_id
      - name: data_source

  - name: data_quality__encounter_flags
    description: One row per clinical encounter input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: encounter_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: encounter_id
      - name: data_source

  - name: data_quality__immunization_flags
    description: One row per clinical immunization input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: immunization_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: immunization_id
      - name: data_source

  - name: data_quality__lab_result_flags
    description: One row per clinical lab result input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: lab_result_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: lab_result_id
      - name: data_source

  - name: data_quality__location_flags
    description: One row per clinical location input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: location_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: location_id
      - name: data_source

  - name: data_quality__medication_flags
    description: One row per clinical medication input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: medication_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: medication_id
      - name: data_source

  - name: data_quality__observation_flags
    description: One row per clinical observation input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: observation_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: observation_id
      - name: data_source

  - name: data_quality__patient_flags
    description: One row per clinical patient input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: patient_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: patient_id
      - name: data_source

  - name: data_quality__practitioner_flags
    description: One row per clinical practitioner input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: practitioner_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: practitioner_id
      - name: data_source

  - name: data_quality__procedure_flags
    description: One row per clinical procedure input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: procedure_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: source_procedure_id
      - name: data_source

  - name: data_quality__provider_attribution_flags
    description: One row per provider-attribution input record per data source with native-grain keys and tri-state logical flags, where 1 means fail, 0 means pass, and null means not applicable.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: provider_attribution_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: member_id
      - name: year_month
      - name: payer
      - name: plan
      - name: data_source
