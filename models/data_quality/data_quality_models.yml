version: 2

models:
  - name: data_quality__structural
    description: One row per data source and input-layer table with structural pass/fail results for table existence, population, expected columns, data types, and primary keys.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural
      tags:
        - data_quality
      materialized: table
    columns:
      - name: data_source
      - name: table_name
      - name: table_exists
      - name: columns_exist
      - name: data_types
      - name: primary_keys
      - name: row_count

  - name: data_quality__structural_expected_columns
    description: One row per enabled Input Layer column expected by the Tuva contract, generated from Input Layer YAML metadata.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_expected_columns
      tags:
        - data_quality
      materialized: table
    columns:
      - name: table_name
      - name: model_name
      - name: column_name
      - name: expected_data_type
      - name: expected_type_family
      - name: required
      - name: is_primary_key
      - name: column_order

  - name: data_quality__structural_actual_columns
    description: One row per actual warehouse column and data source for enabled Input Layer tables, generated through dbt adapter metadata APIs.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_actual_columns
      tags:
        - data_quality
      materialized: table
    columns:
      - name: data_source
      - name: data_source_key
      - name: table_name
      - name: model_name
      - name: column_name
      - name: actual_data_type
      - name: actual_type_family
      - name: table_exists
      - name: row_count

  - name: data_quality__structural_column_details
    description: One row per data source, input-layer table, and expected column with expected-vs-actual column metadata and structural pass/fail results.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_column_details
      tags:
        - data_quality
      materialized: table
    columns:
      - name: data_source
      - name: table
      - name: column
      - name: expected_data_type
      - name: actual_data_type
      - name: required
      - name: is_primary_key
      - name: table_exists
      - name: row_count
      - name: column_exists
      - name: data_type_correct
      - name: column_order

  - name: data_quality__structural_primary_key_tests
    description: One row per primary-key test result for each data source and input-layer table.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: structural_primary_key_tests
      tags:
        - data_quality
      materialized: table
    columns:
      - name: data_source
      - name: table
      - name: column
      - name: test
      - name: test_result

  - name: data_quality__logical
    description: Aggregated logical data quality results derived by summing persisted native-grain flag columns from the logical flag tables.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: logical
      tags:
        - data_quality
      materialized: table
    columns:
      - name: data_source
      - name: table
      - name: test_name
      - name: test_result

  - name: data_quality__medical_claim_line_flags
    description: One row per medical claim line per data source with native-grain keys and 0/1 logical test flags evaluated at the claim-line grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: medical_claim_line_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: claim_line_number
      - name: data_source

  - name: data_quality__medical_claim_claim_flags
    description: One row per medical claim per data source with claim-grain keys and 0/1 logical test flags evaluated at the claim header grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: medical_claim_claim_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: data_source

  - name: data_quality__pharmacy_claim_line_flags
    description: One row per pharmacy claim line per data source with native-grain keys and 0/1 logical test flags evaluated at the claim-line grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: pharmacy_claim_line_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: claim_line_number
      - name: data_source

  - name: data_quality__pharmacy_claim_claim_flags
    description: One row per pharmacy claim per data source with claim-grain keys and 0/1 logical test flags evaluated at the claim header grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: pharmacy_claim_claim_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: claim_id
      - name: data_source

  - name: data_quality__eligibility_span_flags
    description: One row per eligibility span per data source with native-grain keys and 0/1 logical test flags evaluated at the eligibility-span grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: eligibility_span_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: member_id
      - name: enrollment_start_date
      - name: enrollment_end_date
      - name: data_source

  - name: data_quality__eligibility_person_flags
    description: One row per person per data source with 0/1 logical test flags evaluated across eligibility spans for the person.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: eligibility_person_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: person_id
      - name: data_source

  - name: data_quality__appointment_flags
    description: One row per clinical appointment input record per data source with native-grain keys and 0/1 logical test flags evaluated at the appointment-record grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: appointment_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: appointment_id
      - name: data_source

  - name: data_quality__condition_flags
    description: One row per clinical condition input record per data source with native-grain keys and 0/1 logical test flags evaluated at the condition-record grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: condition_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: source_condition_id
      - name: data_source

  - name: data_quality__encounter_flags
    description: One row per clinical encounter input record per data source with native-grain keys and 0/1 logical test flags evaluated at the encounter-record grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: encounter_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: encounter_id
      - name: data_source

  - name: data_quality__immunization_flags
    description: One row per clinical immunization input record per data source with native-grain keys and 0/1 logical test flags evaluated at the immunization-record grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: immunization_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: immunization_id
      - name: data_source

  - name: data_quality__lab_result_flags
    description: One row per clinical lab result input record per data source with native-grain keys and 0/1 logical test flags evaluated at the lab-result-record grain.
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
        {% else %}data_quality{%- endif -%}
      alias: lab_result_flags
      tags:
        - data_quality
      materialized: table
    columns:
      - name: lab_result_id
      - name: data_source
