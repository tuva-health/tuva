version: 2
models:

- name: data_quality__stg_institutional_inpatient
  config:
    schema: |
      {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_data_quality
      {% else %}data_quality{%- endif -%}
    alias: stg_institutional_inpatient
    tags:
      - claims
      - medical
      - stage
    materialized: view