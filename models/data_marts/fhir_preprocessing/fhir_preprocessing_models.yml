version: 2

models:
  ## Final
  - name: fhir_preprocessing__condition
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: condition
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Condition FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: encounter_internal_id
        description: 'Unique identifier for each encounter in the dataset.'
        config:
          meta:
            data_type: integer
      - name: condition_category
        description: >
          Hard-coded to "encounter-diagnosis" for FHIR mapping requirements
          (http://terminology.hl7.org/CodeSystem/condition-category).
        config:
          meta:
            data_type: varchar
      - name: condition_recorded_datetime
        description: 'Date when the condition was recorded.'
        config:
          meta:
            data_type: timestamp
      - name: condition_onset_datetime
        description: 'Date when the condition first occurred.'
        config:
          meta:
            data_type: timestamp
      - name: condition_abatement_datetime
        description: 'Date when the condition was resolved.'
        config:
          meta:
            data_type: timestamp
      - name: condition_clinical_status
        description: 'Status of the record (e.g., condition, test, etc).'
        config:
          meta:
            data_type: varchar
      - name: condition_code
        description: 'The normalized code.'
        config:
          meta:
            data_type: varchar
      - name: condition_code_system
        description: 'The normalized type of code.'
        config:
          meta:
            data_type: varchar
      - name: encounter_status
        description: >
          Hard-coded to "finished" for FHIR mapping requirements
          (http://hl7.org/fhir/encounter-status).
        config:
          meta:
            data_type: integer
      - name: encounter_class_code
        description: >
          Mapped to FHIR requirement based on encounter_group
          (http://terminology.hl7.org/CodeSystem/v3-ActCode).
        config:
          meta:
            data_type: integer
      - name: encounter_start_datetime
        description: 'Date when the encounter started.'
        config:
          meta:
            data_type: timestamp
      - name: encounter_end_datetime
        description: 'Date when the encounter ended.'
        config:
          meta:
            data_type: timestamp
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__coverage
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: coverage
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Coverage FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: organization_name
        description: 'Name of the payer (i.e. health insurer) providing coverage.'
        config:
          meta:
            data_type: varchar
      - name: coverage_plan
        description: 'Name of the plan (i.e. sub contract) providing coverage.'
        config:
          meta:
            data_type: integer
      - name: coverage_period_start
        description: 'Date the patient''s insurance eligibility began.'
        config:
          meta:
            data_type: integer
      - name: coverage_period_end
        description: 'Date the patient''s insurance eligibility ended.'
        config:
          meta:
            data_type: integer
      - name: coverage_relationship
        description: 'The patient''s relationship to the subscriber (e.g., self, spouse,
          child).'
        config:
          meta:
            data_type: integer
      - name: coverage_status
        description: >
          Hard-coded to "active" for FHIR mapping requirements
          (http://hl7.org/fhir/fm-status).
        config:
          meta:
            data_type: integer
      - name: coverage_subscriber_id
        description: 'Identifier that links a patient to a particular insurance product
          or health plan.'
        config:
          meta:
            data_type: integer
      - name: coverage_type_list
        description: >
          Nested list of coverage product type and medical benefit. Mapped
          to  FHIR requirement based on payer_type and coverage_plan
          (http://terminology.hl7.org/CodeSystem/v3-ActCode).
        config:
          meta:
            data_type: integer
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__explanation_of_benefit
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: explanation_of_benefit
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Explanation of Benefit FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: unique_claim_id
        description: 'Unique identifier for a claim. Each claim represents a distinct
          healthcare service or set of services provided to a patient.'
        config:
          meta:
            data_type: varchar
      - name: eob_type_code
        description: 'Indicates whether the claim is professional (CMS-1500),
          institutional (UB-04), dental, or vision.'
        config:
          meta:
            data_type: varchar
      - name: eob_subtype_code
        description: >
          Mapped to FHIR requirement based on encounter_group
          (http://hl7.org/fhir/us/carin-bb/CodeSystem/C4BBInstitutionalClaimSubType).
        config:
          meta:
            data_type: varchar
      - name: eob_billable_period_start
        description: 'The date when the healthcare service was provided. Format:
          YYYY-MM-DD.'
        config:
          meta:
            data_type: varchar
      - name: eob_billable_period_end
        description: 'End date for the claim.'
        config:
          meta:
            data_type: varchar
      - name: eob_created
        description: 'The date the claim was paid.'
        config:
          meta:
            data_type: varchar
      - name: organization_name
        description: 'Name of the payer (i.e. health insurer) providing coverage.'
        config:
          meta:
            data_type: varchar
      - name: practitioner_internal_id
        description: >
          Billing or Rendering Provider ID. Required for FHIR validation,
          defaults to dummy ID "9999999999" if null.
        config:
          meta:
            data_type: varchar
      - name: practitioner_name_text
        description: >
          Billing or Rendering Provider name. Required for FHIR validation,
          defaults to dummy ID "Dummy Practitioner" if null.
        config:
          meta:
            data_type: varchar
      - name: coverage_internal_id
        description: Unique identifier for relevant coverage record.
        config:
          meta:
            data_type: varchar
      - name: eob_diagnosis_list
        description: Nested list of diagnoses related to claim.
        config:
          meta:
            data_type: varchar
      - name: eob_procedure_list
        description: Nested list of procedures related to claim.
        config:
          meta:
            data_type: varchar
      - name: eob_supporting_info_list
        description: >
          Nested list of supporting info related to claim (admission, discharge,
          and bill type for medical claim, or days supply and  refills for
          pharmacy claim).
        config:
          meta:
            data_type: varchar
      - name: eob_item_list
        description: >
          Nested list of claim item details (adjudication, modifiers, revenue
          codes, HCPC/CPT codes, and place of service for medical claim, or
          adjudication and drug codes for pharmacy claim).
        config:
          meta:
            data_type: varchar
      - name: eob_total_list
        description: Nested list of adjudication amounts.
        config:
          meta:
            data_type: varchar
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__medication_dispense
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: medication_dispense
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Medication Dispense FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: medication_dispense_status
        description: >
          Hard-coded to "completed" for FHIR mapping requirements
          (http://terminology.hl7.org/CodeSystem/medicationdispense-status).
        config:
          meta:
            data_type: varchar
      - name: medication_code_system
        description: Normalized code system.
        config:
          meta:
            data_type: varchar
      - name: medication_code
        description: Normalized code.
        config:
          meta:
            data_type: varchar
      - name: medication_code_display
        description: Normalized description.
        config:
          meta:
            data_type: varchar
      - name: medication_dispense_days_supply_value
        description: 'The number of days supply included.'
        config:
          meta:
            data_type: integer
      - name: medication_dispense_when_handed_over
        description: 'Date the medication was dispensed.'
        config:
          meta:
            data_type: varchar
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__observation
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: observation
      tags: fhir_preprocessing
      materialized: table
    description: >
      Final mapping for Observation FHIR resource. Combines observations and lab
      results.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: encounter_internal_id
        description: 'Unique identifier for each encounter in the dataset.'
        config:
          meta:
            data_type: integer
      - name: observation_status
        description: >
          Mapped to FHIR requirement based on lab result status or hard-coded
          to  "final" for observations (http://hl7.org/fhir/observation-status).
        config:
          meta:
            data_type: varchar
      - name: observation_category
        description: >
          Mapped to FHIR requirement based on observation type or hard-coded
          to  "laboratory" (http://hl7.org/fhir/observation-status).
        config:
          meta:
            data_type: varchar
      - name: observation_code_system
        description: Normalized code system.
        config:
          meta:
            data_type: varchar
      - name: observation_code
        description: Normalized code.
        config:
          meta:
            data_type: varchar
      - name: observation_code_text
        description: Normalized description.
        config:
          meta:
            data_type: varchar
      - name: observation_datetime
        description: 'Date of the test result.'
        config:
          meta:
            data_type: timestamp
      - name: observation_value
        description: 'The result of the record (e.g., lab test, observation, etc).'
        config:
          meta:
            data_type: varchar
      - name: observation_value_units
        description: 'Normalized units of the lab test.'
        config:
          meta:
            data_type: number
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__patient
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: patient
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Patient FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: name_first
        description: 'The first name of the patient.'
        config:
          meta:
            data_type: varchar
      - name: name_last
        description: 'The last name of the patient.'
        config:
          meta:
            data_type: varchar
      - name: gender
        description: 'The gender of the patient.'
        config:
          meta:
            data_type: varchar
      - name: race
        description: 'The patient''s race.'
        config:
          meta:
            data_type: varchar
      - name: birth_date
        description: 'The birth date of the patient.'
        config:
          meta:
            data_type: date
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  - name: fhir_preprocessing__procedure
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: procedure
      tags: fhir_preprocessing
      materialized: table
    description: Final mapping for Procedure FHIR resource.
    tests:
      - dbt_utils.unique_combination_of_columns:
          arguments:
            combination_of_columns:
              - patient_internal_id
              - resource_internal_id
    columns:
      - name: patient_internal_id
        description: 'Unique identifier for each person in the dataset.'
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: resource_internal_id
        description: Unique identifier for each record in the table.
        config:
          meta:
            data_type: varchar
            is_primary_key: true
      - name: procedure_status
        description: >
          Hard-coded to "completed" for FHIR mapping requirements
          (http://hl7.org/fhir/event-status).
        config:
          meta:
            data_type: varchar
      - name: procedure_code_system
        description: Normalized code system.
        config:
          meta:
            data_type: varchar
      - name: procedure_code
        description: Normalized code.
        config:
          meta:
            data_type: varchar
      - name: procedure_display
        description: Normalized description.
        config:
          meta:
            data_type: varchar
      - name: procedure_performed_datetime
        description: 'Date when the procedure was performed.'
        config:
          meta:
            data_type: timestamp
      - name: practitioner_npi
        description: 'Unique identifier for the practitioner on record (e.g., ordered
          medication, performed the procedure, etc).'
        config:
          meta:
            data_type: varchar
      - name: data_source
        description: 'User-configured field that indicates the data source.'
        config:
          meta:
            data_type: varchar
      - name: tuva_last_run
        description: 'The last time the data was refreshed. Generated by
          `dbt_utils.pretty_time` as the local time of the `dbt run`
          environment. Timezone is configurable via the `tuva_last_run` var.'
        config:
          meta:
            data_type: timestamp
      - name: tuva_package_version
        description: 'The version defined in the Tuva package dbt_project.yml file.'
        config:
          meta:
            data_type: varchar

  ## Intermediate
  - name: fhir_preprocessing__int_coverage
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_coverage
      tags: fhir_preprocessing
      materialized: table
    description: Prep coverage product for Coverage resource.

  - name: fhir_preprocessing__int_coverage_type
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_coverage_type
      tags: fhir_preprocessing
      materialized: table
    description: Prep coverage type list for the Coverage resource.

  - name: fhir_preprocessing__int_lab_result
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_lab_result
      tags: fhir_preprocessing
      materialized: table
    description: Prep labs to be joined with observations.

  - name: fhir_preprocessing__int_observation
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_observation
      tags: fhir_preprocessing
      materialized: table
    description: Prep observations to be joined with labs.

  - name: fhir_preprocessing__int_medical_claim_diagnosis
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_diagnosis
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep medical claims diagnosis list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_medical_claim_eob
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_eob
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep medical claims to be joined with pharmacy claims for the
      Explanation  of Benefit resource. Combines nested lists for Diagnosis,
      Item, Procedure, Supporting Info, and Total.

  - name: fhir_preprocessing__int_medical_claim_item
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_item
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Item list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_medical_claim_item_adjudication
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_item_adjudication
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep adjudication for claim Item list for the Explanation of Benefit
      resource.

  - name: fhir_preprocessing__int_medical_claim_item_modifier
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_item_modifier
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep modifiers for claim Item list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_medical_claim_procedure
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_procedure
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep procedure list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_medical_claim_supporting_info
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_supporting_info
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Supporting Info list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_medical_claim_total
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_medical_claim_total
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Total list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_pharmacy_claim_eob
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_pharmacy_claim_eob
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep pharmacy claims to be joined with medical claims for the
      Explanation  of Benefit resource. Combines nested lists for Item,
      Supporting Info, and Total.

  - name: fhir_preprocessing__int_pharmacy_claim_item
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_pharmacy_claim_item
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Item list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_pharmacy_claim_item_adjudication
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_pharmacy_claim_item_adjudication
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep adjudication for claim Item list for the Explanation of Benefit
      resource.

  - name: fhir_preprocessing__int_pharmacy_claim_supporting_info
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_pharmacy_claim_supporting_info
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Supporting Info list for the Explanation of Benefit resource.

  - name: fhir_preprocessing__int_pharmacy_claim_total
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _int_pharmacy_claim_total
      tags: fhir_preprocessing
      materialized: table
    description: >
      Prep claim Total list for the Explanation of Benefit resource.

  ## Staging
  - name: fhir_preprocessing__stg_core__condition
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_condition
      tags: fhir_preprocessing
      materialized: view
    description: Staging conditions from core.

  - name: fhir_preprocessing__stg_core__eligibility
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_eligibility
      tags: fhir_preprocessing
      materialized: view
    description: Staging eligibility from core.

  - name: fhir_preprocessing__stg_core__encounter
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_encounter
      tags: fhir_preprocessing
      materialized: view
    description: Staging encounter from core.

  - name: fhir_preprocessing__stg_core__lab_result
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_lab_result
      tags: fhir_preprocessing
      materialized: view
    description: Staging lab results from core.

  - name: fhir_preprocessing__stg_core__medical_claim
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_medical_claim
      tags: fhir_preprocessing
      materialized: view
    description: Staging medical claims from core.

  - name: fhir_preprocessing__stg_core__medication
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_medication
      tags: fhir_preprocessing
      materialized: view
    description: Staging medications from core.

  - name: fhir_preprocessing__stg_core__observation
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_observation
      tags: fhir_preprocessing
      materialized: view
    description: Staging observations from core.

  - name: fhir_preprocessing__stg_core__patient
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_patient
      tags: fhir_preprocessing
      materialized: view
    description: Staging patient from core.

  - name: fhir_preprocessing__stg_core__pharmacy_claim
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_pharmacy_claim
      tags: fhir_preprocessing
      materialized: view
    description: Staging pharmacy claims from core.

  - name: fhir_preprocessing__stg_core__procedure
    config:
      schema: |
        {%- if var('tuva_schema_prefix',None) != None -%}{{var('tuva_schema_prefix')}}_fhir_preprocessing{% else %}fhir_preprocessing{%- endif -%}
      alias: _stg_procedure
      tags: fhir_preprocessing
      materialized: view
    description: Staging procedures from core.
